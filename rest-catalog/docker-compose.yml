services:
#  rest-catalog:
#    image: apache/iceberg-rest-fixture:1.8.1 # https://hub.docker.com/r/apache/iceberg-rest-fixture
#    ports:
#      - "8181:8181"
#    environment:
#      AWS_ACCESS_KEY_ID: minioadmin
#      AWS_SECRET_ACCESS_KEY: minioadmin
#      AWS_REGION: us-east-1
#      CATALOG_WAREHOUSE: s3://warehouse/
#      CATALOG_IO__IMPL: org.apache.iceberg.aws.s3.S3FileIO
#      CATALOG_S3_ENDPOINT: http://localhost:9000
#      CATALOG_S3_PATH_STYLE_ACCESS: 'true'

  minio:
    image: minio/minio:RELEASE.2025-03-12T18-04-18Z # https://hub.docker.com/r/minio/minio
    ports:
      - "9000:9000"
      - "9001:9001"
    command: ["server", "/data", "--console-address", ":9001"]
